{
  "name": "orchestrator-gpt",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "node build-vercel-static.cjs",
    "vercel-build": "npm run build",
    "refresh-roster": "node scripts/refresh-free-roster.mjs",
    "generate:mcp-pages": "node scripts/generate-mcp-pages.mjs",
    "check:mcp-pages": "node scripts/generate-mcp-pages.mjs --check --check-published"
  },
  "dependencies": {
    "open-dashboard-mcp": "0.8.0"
  },
  "devDependencies": {
    "tsx": "4.23.12"
  }
}
