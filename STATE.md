# Current Stage
- Stage: Phase 1 Foundation / Cleanup

# Current State
- Roadmap Source: User-provided `SD Forest - Master Plan for Site Development` (2026-04-05)
- Last Confirmed Completed ROADMAP Point: None confirmed completed yet from the master plan; the repo is still in Phase 1, `Foundation / Cleanup`.
- Summary: The repo now has refreshed canonical memory under `memory/`, one repo-scoped research session in the last 24 hours staged Vercel/FastAPI deployment guidance in `docs/long-term-memory.md`, and the new master plan establishes the active phase sequence for future runs.

# Next Priorities
1. Use Phase 1 of the master plan as the active work queue: define subdomain structure, reduce auth barriers, fix broken navigation, ensure each project has a path back to the hub, identify empty or placeholder projects, fix deployment basics, restore LLM database backend/search functionality, and decide which projects are public, private, or hybrid.
2. Review the staged Vercel deployment findings in `docs/long-term-memory.md` and decide whether they should change the repo's same-origin `/api/*` deployment plan for `sdforest.site`.
3. Confirm the intended production hosting shape for SD Forest: split static plus external FastAPI with rewrites, a `public/` reshuffle for Vercel, or another explicitly documented deployment boundary.
4. Address the highest-value unresolved Phase 1 product gap by restoring `web/llm-db/index.html` backend/search reliability and documenting the required hosted API path.
5. Keep `index.html` and linked product cards aligned with real working routes, honest access expectations, and the new master plan naming changes.

# Run Log
- 2026-04-05: Consolidated the last 24 hours of repo-scoped Codex context, refreshed the three canonical memory files, and replaced the bootstrap roadmap placeholder with the user-provided SD Forest master plan.
- Remaining: no master-plan roadmap point is confirmed completed yet, and the staged Vercel/FastAPI deployment findings still need validation against the repo's real deployment path.
- Immediate next step: use Phase 1 to resolve deployment and navigation truthfulness gaps, starting with the hosted API plan for the LLM database.
